import 'jasmine';

describe('Form#onValidate', () => {
	xit('needs tests', () => {

	})
});

describe('Form#reset', () => {
	xit('needs tests', () => {

	})
});

describe('Form#setValidationResults', () => {
	xit('needs tests', () => {

	})
});

describe('Form#validate', () => {
	xit('needs tests', () => {

	})
});